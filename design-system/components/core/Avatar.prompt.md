User identity chip — shows initials on a brand fill, or a photo when `src` is set.

```jsx
<Avatar name="Alessandro Becker" size="md" />
<Avatar name="Marina Petry" color="purple" />
```

Sizes `sm | md | lg`. Colors `brand | navy | turquoise | purple`.
