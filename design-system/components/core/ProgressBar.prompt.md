Accessible progress track — solid AA-colored fill (fixes the legacy thin light-green ranking bar).

```jsx
<ProgressBar value={9150} max={9150} variant="gradient" label="Sua evolução vs. líder" />
```

Variants `default | success | warning | gradient`. Pass `value`/`max`; percentage is derived.
