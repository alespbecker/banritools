Top application bar — leading slot, brand lockup, spacer, trailing actions.

```jsx
<AppBar
  logo={<img src="assets/logo-mark.svg" width="24" height="24" />}
  leading={<IconButton aria-label="Menu"><Icon name="menu" /></IconButton>}
  actions={<><IconButton aria-label="Atualizar"><Icon name="refresh-cw" /></IconButton><Avatar name="Alessandro" size="sm" /></>}
/>
```
